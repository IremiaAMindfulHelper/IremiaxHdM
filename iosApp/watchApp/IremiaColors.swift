import SwiftUI

extension Color {
    /// Iremia primary teal/petrol — matches the iOS app Primary_500 colorset.
    static let iremiaPrimary = Color(red: 26.0 / 255.0, green: 112.0 / 255.0, blue: 136.0 / 255.0)
    /// Deep petrol used as Watch icon background.
    static let iremiaPetrol = Color(red: 15.0 / 255.0, green: 72.0 / 255.0, blue: 88.0 / 255.0)
    /// Soft teal used for button labels and banner text in the Watch home screen.
    static let iremiaLabel = Color(red: 0x83 / 255.0, green: 0xBA / 255.0, blue: 0xB8 / 255.0)
    /// Dark teal banner gradient seed (#0A5C5A).
    static let iremiaBannerTeal = Color(red: 0x0A / 255.0, green: 0x5C / 255.0, blue: 0x5A / 255.0)
    /// Copper accent used for the central Quick help bubble.
    static let iremiaQuickHelp = Color(red: 0xC8 / 255.0, green: 0x7B / 255.0, blue: 0x3B / 255.0)
}
